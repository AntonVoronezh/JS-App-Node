<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="/css/styles.css">
    <title>Node.js App</title>
</head>

<body>
    <div class="app">
        <div class="app__header">
                <h1> Погода в любом городе</h1>
            <% if (weather) { %>
                <h1><%=weather %></h1>
            <% } %>
            <% if (error) { %>
                <p class="error"><%=error %></h1>
            <% } %>
        </div>
        <form action="/" class="app__content" method="POST">
            <div class="input">
                <label for="city">
                    Город
                </label>
                <input type="text" id="city" placeholder="Название города" name="city">
            </div>
            <button type="submit" class="btn">Показать погоду</button>
        </form>
    </div>
</body>

</html>